FROM node:20-alpine

WORKDIR /app

# For the time being, just provide a simple message
RUN echo "Frontend container ready. Implementation pending." > /app/README.md

# Command to keep container running
CMD ["tail", "-f", "/dev/null"]
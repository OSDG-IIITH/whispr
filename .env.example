# Whispr Environment Variables

# See frontend/.env.example for the full list of environment variables
# Copy frontend/.env.example to frontend/.env.local and fill in the values

# The main environment file is in the frontend directory

# Whispr Environment Variables

# See app/.env.example for the full list of environment variables
# Copy app/.env.example to app/.env.local and fill in the values

# The main environment file is in the app directory
